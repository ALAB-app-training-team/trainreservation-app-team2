<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis/org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alab.shinkansendego.features.schedule.repositories.DepartureArrivalTimeRepository">
    <select id="findScheduleBySectionKmCd"
            resultType="com.alab.shinkansendego.features.schedule.dtos.DepartureArrivalTimeDto">
        SELECT schedule_cd,departure_time,arrival_time
        FROM M_DepartureArrivalTime WHERE section_cd=#{section_cd};
    </select>
    <select id="findScheduleBySectionKmCdAndScheduleCd"
            resultType="com.alab.shinkansendego.features.schedule.entities.DepartureArrivalTimeEntity">
        SELECT schedule_cd,departure_time,arrival_time
        FROM M_DepartureArrivalTime WHERE schedule_cd=#{schedule_cd}
        AND section_cd IN
        <foreach item="item" collection="section_cd_list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
    <select id="findSectionCdByScheduleCd"
            resultType="String">
        SELECT section_cd
        FROM M_DepartureArrivalTime WHERE schedule_cd=#{schedule_cd} AND departure_time>=#{departure_time} AND
        #{arrival_time}>=arrival_time;
    </select>
</mapper>

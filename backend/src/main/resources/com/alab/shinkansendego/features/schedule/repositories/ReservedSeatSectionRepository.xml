<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis/org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alab.shinkansendego.features.schedule.repositories.ReservedSeatSectionRepository">
    <select id="findReservedSeatCdOfTrainCarBySectionCd" resultType="String">
        SELECT seat_cd
        FROM T_ReservedSeatSection
        WHERE ride_date=#{ride_date} AND schedule_cd=#{schedule_cd} AND train_car_cd=#{train_car_cd} AND
        reserved_section_cd=#{reserved_section_cd}
        ORDER BY seat_cd;
    </select>
    <insert id="insertReservedSeatSections"
            parameterType="java.util.List">
        INSERT INTO T_ReservedSeatSection (id, purchase_id, ride_date, schedule_cd,
        train_car_cd,seat_cd,reserved_section_cd)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id, typeHandler=org.apache.ibatis.type.ObjectTypeHandler},
            #{item.purchase_id, typeHandler=org.apache.ibatis.type.ObjectTypeHandler},
            #{item.ride_date},
            #{item.schedule_cd},
            #{item.train_car_cd},
            #{item.seat_cd},
            #{item.reserved_section_cd}
            )
        </foreach>
    </insert>
</mapper>
